# Domain docs

This repo uses a single-context layout:
CONTEXT.md at the root and decisions under docs/adr/.

Before exploring, read CONTEXT.md and ADRs relevant to the area.
If these files are absent, proceed silently. The domain-modeling
skill creates them when terms or decisions are resolved.

Use the glossary's vocabulary when naming domain concepts.
Surface conflicts with existing ADRs explicitly.
